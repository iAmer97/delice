<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="android.example.delice">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Delice"
        tools:replace="android:theme">
        <activity android:name="android.example.delice.DmActivity"></activity>
        <activity android:name="android.example.delice.StoryActivity" />
        <activity
            android:name="android.example.delice.ChatActivity"
            android:parentActivityName="android.example.delice.DmActivity" />
        <activity android:name="android.example.delice.AddStoryActivity" />
        <activity android:name="android.example.delice.CartActivity" />
        <activity android:name="android.example.delice.Model.ShoppingCart" />
        <activity android:name="android.example.delice.PostingActivity" />
        <activity android:name="android.example.delice.OptionsActivity" />
        <activity android:name="android.example.delice.FollowingActivity" />
        <activity android:name="android.example.delice.EditProfileActivity" />
        <activity android:name="android.example.delice.CommentsActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name="android.example.delice.MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name="android.example.delice.RegisterActivity" />
        <activity android:name="android.example.delice.LoginActivity" />
        <activity android:name="android.example.delice.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>